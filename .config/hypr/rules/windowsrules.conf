# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
#
# Autor =  Shen Belmonth 
# Followme = https://github.com/Shentxt 
#
# ██╗    ██╗██╗███╗   ██╗██████╗  ██████╗ ██╗    ██╗██████╗ ██╗   ██╗██╗     ███████╗███████╗
# ██║    ██║██║████╗  ██║██╔══██╗██╔═══██╗██║    ██║██╔══██╗██║   ██║██║     ██╔════╝██╔════╝
# ██║ █╗ ██║██║██╔██╗ ██║██║  ██║██║   ██║██║ █╗ ██║██████╔╝██║   ██║██║     █████╗  ███████╗
# ██║███╗██║██║██║╚██╗██║██║  ██║██║   ██║██║███╗██║██╔══██╗██║   ██║██║     ██╔══╝  ╚════██║
# ╚███╔███╔╝██║██║ ╚████║██████╔╝╚██████╔╝╚███╔███╔╝██║  ██║╚██████╔╝███████╗███████╗███████║
# ╚══╝╚══╝ ╚═╝╚═╝  ╚═══╝╚═════╝  ╚═════╝  ╚══╝╚══╝ ╚═╝  ╚═╝ ╚═════╝ ╚══════╝╚══════╝╚══════╝
#                                  
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
#           Example Window Rules
#
# this section are the rules of hyprland, 
# either the application or the window
# any questions visit the wiki, especially this section.
# wiki: https://wiki.hyprland.org/Configuring/Window-Rules/
# wiki: https://wiki.hyprland.org/Configuring/Keywords/ 
#
# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

# for windows named/classed as abc and xyz
# windowrule = move 69 420,abc
# windowrule = size 420 69,abc
# windowrule = tile,xyz
# windowrule = float,abc
# windowrule = pseudo,abc
# windowrule = monitor 0,xyz

# windows and monitor
  monitor=,preferred,auto,auto

$size = size 833 560
$move = move 261 110
$msize = size 339 174
$mmove = move 997 516

# - - - - - - - - - - - - - - - - - - - -
#     Aplications Rules
#
# tip, braces is to separate it from other rules. 
# since the # rules must be separated and cannot 
# be done on the same line
#
# - - - - - - - - - - - - - - - - - - - -

# Aplications

retro{
windowrule = fullscreen,^(org.libretro.RetroArch)$
windowrule = opaque,^(org.libretro.RetroArch)$
}

nwg-look{
 windowrule = float,^(nwg-look)$
 windowrule = $size,^(nwg-look)$
 windowrule = $move,^(nwg-look)$
}

swayimg{
 windowrule = float,^(swayimg_.*)$
 windowrule = $size,^(swayimg_.*)$
 windowrule = $move,^(swayimg_.*)$
}

viewnior{
 windowrule = float,^(Viewnior)$
 windowrule = $size,^(Viewnior)$
 windowrule = $move,^(Viewnior)$
}

feh{
 windowrule = float,^(feh)$
 windowrule = $size,^(feh)$
 windowrule = $move,^(feg)$
}

blueman-manager{
 windowrule = float,^(blueman-manager)$
 windowrule = $size,^(blueman-manager)$
 windowrule = $move,^(blueman-manager)$
}

blueberry.py{
 windowrule = float,^(blueberry.py)$
 windowrule = $size,^(blueberry.py)$
 windowrule = $move,^(blueberry.py)$
}

config{
 windowrule = float,^(org.gnome.Settings)$
 windowrule = $size,^(org.gnome.Settings)$
 windowrule = $move,^(org.gnome.Settings)$
}

roller{
 windowrule = float,^(org.gnome.FileRoller)$
 windowrule = $size,^(org.gnome.FileRoller)$
 windowrule = $move,^(org.gnome.FileRoller)$
}

thunar{
 windowrule = float,^(thunar)$
 windowrule = $size,^(thunar)$
 windowrule = $move,^(thunar)$
}

nautilus{
 windowrule = float,^(org.gnome.Nautilus)$
 windowrule = $size,^(org.gnome.Nautilus)$
 windowrule = $move,^(org.gnome.Nautilus)$
}

control{
 windowrule = float,^(org.corectrl.CoreCtrl)$
 windowrule = $size,^(org.corectrl.CoreCtrl)$
 windowrule = $move,^(org.corectrl.CoreCtrl)$
}

pavucontrol{
 windowrule = float,^(org.pulseaudio.pavucontrol)$
 windowrule = $size,^(org.pulseaudio.pavucontrol)$
 windowrule = $move,^(org.pulseaudio.pavucontrol)$
}

bauh{
 windowrule = float,^(python3)$
 windowrule = $size,^(python3)$
 windowrule = $move,^(python3)$
}

bleachbit{
 windowrule = float,^(bleachbit)$
 windowrule = $size,^(bleachbit)$
 windowrule = $move,^(bleachbit)$
}

sddm-config-editor{
 windowrule = float,^(sddm-config-editor)$
 windowrule = $size,^(sddm-config-editor)$
 windowrule = $move,^(sddm-config-editor)$
}

steam{
 windowrule = float,^(steam)$
 windowrule = $size,^(steam)$
 windowrule = $move,^(steam)$
}

qt5ct{
 windowrule = float,^(qt5ct)$
 windowrule = $size,^(qt5ct)$
 windowrule = $move,^(qt5ct)$
}

kvantummanager{
 windowrule = float,^(kvantummanager)$
 windowrule = $size,^(kvantummanager)$
 windowrule = $move,^(kvantummanager)$
}

zenity{
 windowrule = float,^(zenity)$
 windowrule = $size,^(zenity)$
 windowrule = $move,^(zenity)$
}

thunderbird{
 windowrule = float,^(thunderbird)$
 windowrule = $size,^(thunderbird)$
 windowrule = $move,^(thunderbird)$
}

birdtray{
 windowrule = float,^(com.ulduzsoft.birdtray)$
 windowrule = $size,^(com.ulduzsoft.birdtray)$
 windowrule = $move,^(com.ulduzsoft.birdtray)$
}

clamat{
 windowrule = float,^(clamtk)$
 windowrule = $size,^(clamtk)$
 windowrule = $move,^(clamtk)$
}

# - - - - - - - - - - - - - - - - - - - -
#     Popup Rules
#
# I did it this way so that it only affects firefox,
# popups and not firefox itself
# keep in mind: do it this way if you want to affect 
# something within an application 
# and not the application itself
#
# - - - - - - - - - - - - - - - - - - - -

kitty-float{
 windowrulev2 = float, class:^(kitty)$, title:^(Float)$
 windowrulev2 = $size, class:^(kitty)$, title:^(Float)$
 windowrulev2 = $move, class:^(kitty)$, title:^(Float)$
}

virtualbox-conf{
 windowrulev2 = float, class:^(VirtualBox)$, title:^(Practicas - Configuración)$
 windowrulev2 = $size, class:^(VirtualBox)$, title:^(Practicas - Configuración)$
 windowrulev2 = $move, class:^(VirtualBox)$, title:^(Practicas - Configuración)$
}

firefox-dialog{
 windowrulev2 = float, class:^(firefox)$, title:^(Picture-in-picture)$
 windowrulev2 = $msize, class:^(firefox)$, title:^(Picture-in-picture)$
 windowrulev2 = $mmove, class:^(firefox)$, title:^(Picture-in-picture)$
 windowrulev2 = opaque, class:^(firefox)$, title:^(Picture-in-picture)$
 windowrulev2 = pin, class:^(firefox)$, title:^(Picture-in-picture)$
}

firefox-filemanager{
 windowrulev2 = float, class:^(firefox)$, title:^(Subir archivo)$
 windowrulev2 = $size, class:^(firefox)$, title:^(Subir archivo)$
 windowrulev2 = $move, class:^(firefox)$, title:^(Subir archivo)$
}

firefox-bi{
 windowrulev2 = float, class:^(firefox)$, title:^(Biblioteca)$
 windowrulev2 = $size, class:^(firefox)$, title:^(Biblioteca)$
 windowrulev2 = $move, class:^(firefox)$, title:^(Biblioteca)$
}

heroic{
 windowrulev2 = float, class:^(heroic)$, title:^(heroic)$
 windowrulev2 = $size, class:^(heroic)$, title:^(heroic)$
 windowrulev2 = $move, class:^(heroic)$, title:^(heroic)$
}

mpv {
 windowrule = float,^(mpv)$
 windowrule = $msize,^(mpv)$
 windowrule = $mmove,^(mpv)$
 windowrule = pin,^(mpv)$
 windowrule = opaque,^(mpv)$
}

rofi{
# will only affect rofi-lbonn-wayland
 layerrule = blur,^(rofi)$
}

wofi{
 layerrule = blur,^(wofi)$
}

gtk{
  layerrule = blur,^(gtk-layer-shell)$
# layerrule = ignorezero,^(gtk-layer-shell)$
# layerrule = ignorealpha,^(gtk-layer-shell)$
}

eww {
 layerrule = blur,^(eww)$
 layerrule = ignorezero,^(eww)$
 layerrule = ignorealpha,^(eww)$
}
